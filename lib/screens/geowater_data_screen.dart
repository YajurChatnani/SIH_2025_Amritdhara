import 'package:flutter/material.dart';
class GeowaterDataScreen extends StatelessWidget {
  const GeowaterDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Geowater Data Screen"),
      ),
    );
  }
}
